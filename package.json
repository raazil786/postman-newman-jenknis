{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "\"project for storing postman tests\"",
  "main": "index.js",
  "scripts": {
    "api-tests": "newman run SMOKE_API3.0.postman_collection.json -e Razil_PROD.postman_environment.json --reporters cli,junit --reporter-junit-export rawResults.xml"
  },
  "author": "razil",
  "dependencies": {
    "newman": "^3.8.3"
  }
}
